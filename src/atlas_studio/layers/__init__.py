"""Atlas Studio's explicit platform layers.

New platform behavior belongs in a layer module instead of expanding the web
entrypoint.  The API composes these layers; it does not own their policy.
"""

